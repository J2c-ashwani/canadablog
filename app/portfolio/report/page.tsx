import ReportClient from "./ReportClient"

export const metadata = {
  title: "Executive Funding Opportunity Assessment | FSI Digital",
  description: "Your verified funding readiness score, matched grants list, and priority stacking strategy roadmap.",
}

export default function ReportPage() {
  return <ReportClient />
}
